{
  "name": "prediction-market-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for decentralized prediction market",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy-sepolia.js --network hardhat",
    "verify:sepolia": "hardhat verify --network sepolia",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.0.0"
  }
}